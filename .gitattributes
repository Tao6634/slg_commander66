# 统一换行符，避免不同系统 clone 后出现「整个文件都变了」的假 diff
* text=auto

# .bat 在 Windows 上必须是 CRLF，否则可能执行异常
*.bat  text eol=crlf
*.cmd  text eol=crlf

# 脚本与文档用 LF
*.py   text eol=lf
*.pyw  text eol=lf
*.md   text eol=lf
*.txt  text eol=lf

# 二进制，别动
*.png  binary
*.ico  binary
*.exe  binary
*.save binary
